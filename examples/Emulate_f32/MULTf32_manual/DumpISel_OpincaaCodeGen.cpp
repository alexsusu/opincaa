// Code auto-generated by method Kernel::genLLVMISelManualCode()
//   from the OPINCAA lib, from kernel mult.f32.
// You should include this code in the Select() method of the [Target]SelectionDAGISel
//   class of your back end (or MAYBE in the ISelLowering pass).
// Number of instructions generated: 373.




SDValue ct0 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R31 = 0;// Instr #0
SDNode *vload0 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct0,
                                    // glue (or chain) input edge
                                    SDValue(nodeOpSrcCast1, 1)
                                    );

SDValue ct1 = CrtDAG->getConstant(1, DL, MVT::i16, true, false);
// R30 = 1;// Instr #1
SDNode *vload1 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct1,
                                    // glue (or chain) input edge
                                    SDValue(vload0, 1)
                                    );

SDValue ct2 = CrtDAG->getConstant(16, DL, MVT::i16, true, false);
// R29 = 16;// Instr #2
SDNode *vload2 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct2,
                                    // glue (or chain) input edge
                                    SDValue(vload1, 1)
                                    );

SDValue ct3 = CrtDAG->getConstant(255, DL, MVT::i16, true, false);
// R28 = 255;// Instr #3
SDNode *vload3 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct3,
                                    // glue (or chain) input edge
                                    SDValue(vload2, 1)
                                    );

SDValue ct4 = CrtDAG->getConstant(127, DL, MVT::i16, true, false);
// R10 = 127;// Instr #4
SDNode *vload4 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct4,
                                    // glue (or chain) input edge
                                    SDValue(vload3, 1)
                                    );

SDValue ct5 = CrtDAG->getConstant(32640, DL, MVT::i16, true, false);
// R09 = 32640;// Instr #5
SDNode *vload5 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct5,
                                    // glue (or chain) input edge
                                    SDValue(vload4, 1)
                                    );

SDValue ct6 = CrtDAG->getConstant(-32768, DL, MVT::i16, true, false);
// R08 = -32768;// Instr #6
SDNode *vload6 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct6,
                                    // glue (or chain) input edge
                                    SDValue(vload5, 1)
                                    );

SDValue ct7 = CrtDAG->getConstant(128, DL, MVT::i16, true, false);
// R07 = 128;// Instr #7
SDNode *vload7 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct7,
                                    // glue (or chain) input edge
                                    SDValue(vload6, 1)
                                    );

SDValue ct8 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R55 = 0;// Instr #8
SDNode *vload8 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct8,
                                    // glue (or chain) input edge
                                    SDValue(vload7, 1)
                                    );

SDValue ct9 = CrtDAG->getConstant(1, DL, MVT::i16, true, false);
// R00 = 1;// Instr #9
SDNode *vload9 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct9,
                                    // glue (or chain) input edge
                                    SDValue(vload8, 1)
                                    );

// R41 = INDEX;// Instr #10
SDNode *ldix0 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload9, 1)
                                    );

// R38 = R41 & R30;// Instr #11
SDNode *and0 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix0, 1)
                                    );

// R34 = R38 == R30;// Instr #12
SDNode *eq0 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and0, 1)
                                    );

SDValue ct10 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #13
SDNode *nop0 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct10,
                                    // glue (or chain) input edge
                                    SDValue(eq0, 1)
                                    );

// WHERE_EQ;// Instr #14
SDNode *whereeq0 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop0, 0)
                                    );

SDValue ct11 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #15
SDNode *vload10 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct11,
                                    SDValue(vload9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq0, 0)
                                    );

// END_WHERE;// Instr #16
SDNode *endwhere0 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload10, 1)
                                    );

// R43 = R27 | R27;// Instr #17
SDNode *or0 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere0, 0)
                                    );

// CELL_SHL(R27, R30);// Instr #18
SDNode *cellshl0 = CrtDAG->getMachineNode(
                                    Connex::CELLSHL_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or0, 1)
                                    );

SDValue ct12 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #19
SDNode *nop1 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct12,
                                    // glue (or chain) input edge
                                    SDValue(cellshl0, 0)
                                    );

// R27 = SHIFT_REG;// Instr #20
SDNode *ldsh0 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop1, 0)
                                    );

// R24 = R27 & R08;// Instr #21
SDNode *and1 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(ldsh0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh0, 1)
                                    );

// R25 = R27 & R09;// Instr #22
SDNode *and2 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(ldsh0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and1, 1)
                                    );

SDValue ct13 = CrtDAG->getConstant(7, DL, MVT::i16, true, false);
// R25 = R25 >> 7;// Instr #23
SDNode *ishr0 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and2, 0),
                                    ct13,
                                    // glue (or chain) input edge
                                    SDValue(and2, 1)
                                    );

SDValue ct14 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R26 = R27 << 0;// Instr #24
SDNode *ishl0 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh0, 0),
                                    ct14,
                                    // glue (or chain) input edge
                                    SDValue(ishr0, 1)
                                    );

// R12 = INDEX;// Instr #25
SDNode *ldix1 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl0, 1)
                                    );

// R12 = R12 & R30;// Instr #26
SDNode *and3 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix1, 1)
                                    );

// R14 = R12 == R31;// Instr #27
SDNode *eq1 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and3, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and3, 1)
                                    );

SDValue ct15 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #28
SDNode *nop2 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct15,
                                    // glue (or chain) input edge
                                    SDValue(eq1, 1)
                                    );

// WHERE_EQ;// Instr #29
SDNode *whereeq1 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop2, 0)
                                    );

// R26 = R27 & R10;// Instr #30
SDNode *and4 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload4, 0),
                                    SDValue(ldsh0, 0),
                                    SDValue(ishl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq1, 0)
                                    }
                                    );

// END_WHERE;// Instr #31
SDNode *endwhere1 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and4, 1)
                                    );

// R12 = R43 | R26;// Instr #32
SDNode *or1 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere1, 0)
                                    );

// R14 = R31 == R12;// Instr #33
SDNode *eq2 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(or1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or1, 1)
                                    );

// R14 = R30 - R14;// Instr #34
SDNode *sub0 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq2, 1)
                                    );

// R13 = R25 == R31;// Instr #35
SDNode *eq3 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr0, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub0, 1)
                                    );

// R12 = R13 & R14;// Instr #36
SDNode *and5 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub0, 0),
                                    SDValue(eq3, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq3, 1)
                                    );

// R12 = R12 == R30;// Instr #37
SDNode *eq4 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and5, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and5, 1)
                                    );

SDValue ct16 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #38
SDNode *nop3 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct16,
                                    // glue (or chain) input edge
                                    SDValue(eq4, 1)
                                    );

// WHERE_EQ;// Instr #39
SDNode *whereeq2 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop3, 0)
                                    );

SDValue ct17 = CrtDAG->getConstant(1, DL, MVT::i16, true, false);
// R25 = 1;// Instr #40
SDNode *vload11 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct17,
                                    SDValue(ishr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq2, 0)
                                    );

// END_WHERE;// Instr #41
SDNode *endwhere2 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload11, 1)
                                    );

// R14 = R25 == R28;// Instr #42
SDNode *eq5 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload11, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere2, 0)
                                    );

// R14 = R14 | R13;// Instr #43
SDNode *or2 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq3, 0),
                                    SDValue(eq5, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq5, 1)
                                    );

// R14 = R14 == R31;// Instr #44
SDNode *eq6 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or2, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(or2, 1)
                                    );

SDValue ct18 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #45
SDNode *nop4 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct18,
                                    // glue (or chain) input edge
                                    SDValue(eq6, 1)
                                    );

// WHERE_EQ;// Instr #46
SDNode *whereeq3 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop4, 0)
                                    );

// R26 = R26 | R07;// Instr #47
SDNode *or3 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(and4, 0),
                                    SDValue(and4, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq3, 0)
                                    }
                                    );

// END_WHERE;// Instr #48
SDNode *endwhere3 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or3, 1)
                                    );

// R42 = R23 | R23;// Instr #49
SDNode *or4 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere3, 0)
                                    );

// CELL_SHL(R23, R30);// Instr #50
SDNode *cellshl1 = CrtDAG->getMachineNode(
                                    Connex::CELLSHL_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or4, 1)
                                    );

SDValue ct19 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #51
SDNode *nop5 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct19,
                                    // glue (or chain) input edge
                                    SDValue(cellshl1, 0)
                                    );

// R23 = SHIFT_REG;// Instr #52
SDNode *ldsh1 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop5, 0)
                                    );

// R20 = R23 & R08;// Instr #53
SDNode *and6 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(ldsh1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh1, 1)
                                    );

// R21 = R23 & R09;// Instr #54
SDNode *and7 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(ldsh1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and6, 1)
                                    );

SDValue ct20 = CrtDAG->getConstant(7, DL, MVT::i16, true, false);
// R21 = R21 >> 7;// Instr #55
SDNode *ishr1 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and7, 0),
                                    ct20,
                                    // glue (or chain) input edge
                                    SDValue(and7, 1)
                                    );

SDValue ct21 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R22 = R23 << 0;// Instr #56
SDNode *ishl1 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh1, 0),
                                    ct21,
                                    // glue (or chain) input edge
                                    SDValue(ishr1, 1)
                                    );

// R12 = INDEX;// Instr #57
SDNode *ldix2 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl1, 1)
                                    );

// R12 = R12 & R30;// Instr #58
SDNode *and8 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix2, 1)
                                    );

// R14 = R12 == R31;// Instr #59
SDNode *eq7 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and8, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and8, 1)
                                    );

SDValue ct22 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #60
SDNode *nop6 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct22,
                                    // glue (or chain) input edge
                                    SDValue(eq7, 1)
                                    );

// WHERE_EQ;// Instr #61
SDNode *whereeq4 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop6, 0)
                                    );

// R22 = R23 & R10;// Instr #62
SDNode *and9 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload4, 0),
                                    SDValue(ldsh1, 0),
                                    SDValue(ishl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq4, 0)
                                    }
                                    );

// END_WHERE;// Instr #63
SDNode *endwhere4 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and9, 1)
                                    );

// R12 = R42 | R22;// Instr #64
SDNode *or5 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and9, 0),
                                    SDValue(or4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere4, 0)
                                    );

// R14 = R31 == R12;// Instr #65
SDNode *eq8 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(or5, 0),
                                    // glue (or chain) input edge
                                    SDValue(or5, 1)
                                    );

// R14 = R30 - R14;// Instr #66
SDNode *sub1 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq8, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq8, 1)
                                    );

// R13 = R21 == R31;// Instr #67
SDNode *eq9 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr1, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub1, 1)
                                    );

// R12 = R13 & R14;// Instr #68
SDNode *and10 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(eq9, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq9, 1)
                                    );

// R12 = R12 == R30;// Instr #69
SDNode *eq10 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and10, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and10, 1)
                                    );

SDValue ct23 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #70
SDNode *nop7 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct23,
                                    // glue (or chain) input edge
                                    SDValue(eq10, 1)
                                    );

// WHERE_EQ;// Instr #71
SDNode *whereeq5 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop7, 0)
                                    );

SDValue ct24 = CrtDAG->getConstant(1, DL, MVT::i16, true, false);
// R21 = 1;// Instr #72
SDNode *vload12 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct24,
                                    SDValue(ishr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq5, 0)
                                    );

// END_WHERE;// Instr #73
SDNode *endwhere5 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload12, 1)
                                    );

// R14 = R21 == R28;// Instr #74
SDNode *eq11 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload12, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere5, 0)
                                    );

// R14 = R14 | R13;// Instr #75
SDNode *or6 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq9, 0),
                                    SDValue(eq11, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq11, 1)
                                    );

// R14 = R14 == R31;// Instr #76
SDNode *eq12 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or6, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(or6, 1)
                                    );

SDValue ct25 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #77
SDNode *nop8 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct25,
                                    // glue (or chain) input edge
                                    SDValue(eq12, 1)
                                    );

// WHERE_EQ;// Instr #78
SDNode *whereeq6 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop8, 0)
                                    );

// R22 = R22 | R07;// Instr #79
SDNode *or7 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(and9, 0),
                                    SDValue(and9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq6, 0)
                                    }
                                    );

// END_WHERE;// Instr #80
SDNode *endwhere6 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or7, 1)
                                    );

SDValue ct26 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R19 = 0;// Instr #81
SDNode *vload13 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct26,
                                    // glue (or chain) input edge
                                    SDValue(endwhere6, 0)
                                    );

// R41 = INDEX;// Instr #82
SDNode *ldix3 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload13, 1)
                                    );

// R38 = R41 & R30;// Instr #83
SDNode *and11 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix3, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix3, 1)
                                    );

// R34 = R38 == R30;// Instr #84
SDNode *eq13 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and11, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and11, 1)
                                    );

SDValue ct27 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #85
SDNode *nop9 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct27,
                                    // glue (or chain) input edge
                                    SDValue(eq13, 1)
                                    );

// WHERE_EQ;// Instr #86
SDNode *whereeq7 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop9, 0)
                                    );

SDValue ct28 = CrtDAG->getConstant(32640, DL, MVT::i16, true, false);
// R19 = 32640;// Instr #87
SDNode *vload14 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct28,
                                    SDValue(vload13, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq7, 0)
                                    );

// END_WHERE;// Instr #88
SDNode *endwhere7 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload14, 1)
                                    );

// R41 = INDEX;// Instr #89
SDNode *ldix4 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(endwhere7, 0)
                                    );

// R38 = R41 & R30;// Instr #90
SDNode *and12 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix4, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix4, 1)
                                    );

// R34 = R38 == R31;// Instr #91
SDNode *eq14 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and12, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and12, 1)
                                    );

SDValue ct29 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #92
SDNode *nop10 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct29,
                                    // glue (or chain) input edge
                                    SDValue(eq14, 1)
                                    );

// WHERE_EQ;// Instr #93
SDNode *whereeq8 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop10, 0)
                                    );

// R15 = R24 ^ R20;// Instr #94
SDNode *xor0 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and6, 0),
                                    SDValue(and1, 0),
                                    SDValue(, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq8, 0)
                                    }
                                    );

// END_WHERE;// Instr #95
SDNode *endwhere8 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(xor0, 1)
                                    );

SDValue ct30 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R16 = 0;// Instr #96
SDNode *vload15 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct30,
                                    // glue (or chain) input edge
                                    SDValue(endwhere8, 0)
                                    );

SDValue ct31 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #97
SDNode *vload16 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct31,
                                    // glue (or chain) input edge
                                    SDValue(vload15, 1)
                                    );

SDValue ct32 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R17 = 0;// Instr #98
SDNode *vload17 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct32,
                                    // glue (or chain) input edge
                                    SDValue(vload16, 1)
                                    );

SDValue ct33 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R54 = 0;// Instr #99
SDNode *vload18 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct33,
                                    // glue (or chain) input edge
                                    SDValue(vload17, 1)
                                    );

SDValue ct34 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R60 = 0;// Instr #100
SDNode *vload19 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct34,
                                    // glue (or chain) input edge
                                    SDValue(vload18, 1)
                                    );

SDValue ct35 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R59 = 0;// Instr #101
SDNode *vload20 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct35,
                                    // glue (or chain) input edge
                                    SDValue(vload19, 1)
                                    );

SDValue ct36 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R58 = 0;// Instr #102
SDNode *vload21 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct36,
                                    // glue (or chain) input edge
                                    SDValue(vload20, 1)
                                    );

SDValue ct37 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R57 = 0;// Instr #103
SDNode *vload22 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct37,
                                    // glue (or chain) input edge
                                    SDValue(vload21, 1)
                                    );

// R02 = R27 == R24;// Instr #104
SDNode *eq15 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh0, 0),
                                    SDValue(and1, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload22, 1)
                                    );

// R01 = R23 == R20;// Instr #105
SDNode *eq16 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh1, 0),
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq15, 1)
                                    );

// R36 = R25 == R28;// Instr #106
SDNode *eq17 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload11, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq16, 1)
                                    );

// R38 = R31 < R26;// Instr #107
SDNode *lt0 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq17, 1)
                                    );

// R35 = R21 == R28;// Instr #108
SDNode *eq18 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload12, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt0, 1)
                                    );

// R37 = R31 < R22;// Instr #109
SDNode *lt1 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(or7, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq18, 1)
                                    );

// R62 = R36 & R38;// Instr #110
SDNode *and13 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt0, 0),
                                    SDValue(eq17, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt1, 1)
                                    );

// R63 = R35 & R37;// Instr #111
SDNode *and14 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt1, 0),
                                    SDValue(eq18, 0),
                                    // glue (or chain) input edge
                                    SDValue(and13, 1)
                                    );

// R61 = R62 | R63;// Instr #112
SDNode *or8 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and14, 0),
                                    SDValue(and13, 0),
                                    // glue (or chain) input edge
                                    SDValue(and14, 1)
                                    );

// R62 = R36 & R01;// Instr #113
SDNode *and15 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq16, 0),
                                    SDValue(eq17, 0),
                                    // glue (or chain) input edge
                                    SDValue(or8, 1)
                                    );

// R63 = R35 & R02;// Instr #114
SDNode *and16 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq15, 0),
                                    SDValue(eq18, 0),
                                    // glue (or chain) input edge
                                    SDValue(and15, 1)
                                    );

// R61 = R61 | R62;// Instr #115
SDNode *or9 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and15, 0),
                                    SDValue(or8, 0),
                                    // glue (or chain) input edge
                                    SDValue(and16, 1)
                                    );

// R61 = R61 | R63;// Instr #116
SDNode *or10 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and16, 0),
                                    SDValue(or9, 0),
                                    // glue (or chain) input edge
                                    SDValue(or9, 1)
                                    );

// R03 = R61 == R30;// Instr #117
SDNode *eq19 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or10, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or10, 1)
                                    );

SDValue ct38 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #118
SDNode *nop11 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct38,
                                    // glue (or chain) input edge
                                    SDValue(eq19, 1)
                                    );

// WHERE_EQ;// Instr #119
SDNode *whereeq9 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop11, 0)
                                    );

SDValue ct39 = CrtDAG->getConstant(1, DL, MVT::i16, true, false);
// R19 = 1;// Instr #120
SDNode *vload23 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct39,
                                    SDValue(vload14, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq9, 0)
                                    );

SDValue ct40 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #121
SDNode *vload24 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct40,
                                    SDValue(vload10, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload23, 1)
                                    );

// END_WHERE;// Instr #122
SDNode *endwhere9 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload24, 1)
                                    );

// R03 = R61 == R30;// Instr #123
SDNode *eq20 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or10, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere9, 0)
                                    );

// CELL_SHR(R03, R30);// Instr #124
SDNode *cellshr0 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(eq20, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq20, 1)
                                    );

SDValue ct41 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #125
SDNode *nop12 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct41,
                                    // glue (or chain) input edge
                                    SDValue(cellshr0, 0)
                                    );

// R04 = SHIFT_REG;// Instr #126
SDNode *ldsh2 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop12, 0)
                                    );

// R41 = INDEX;// Instr #127
SDNode *ldix5 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ldsh2, 1)
                                    );

// R38 = R41 & R30;// Instr #128
SDNode *and17 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix5, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix5, 1)
                                    );

// R34 = R38 == R30;// Instr #129
SDNode *eq21 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and17, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and17, 1)
                                    );

// R04 = R04 & R34;// Instr #130
SDNode *and18 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq21, 0),
                                    SDValue(ldsh2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq21, 1)
                                    );

// R04 = R04 == R30;// Instr #131
SDNode *eq22 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and18, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and18, 1)
                                    );

SDValue ct42 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #132
SDNode *nop13 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct42,
                                    // glue (or chain) input edge
                                    SDValue(eq22, 1)
                                    );

// WHERE_EQ;// Instr #133
SDNode *whereeq10 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop13, 0)
                                    );

SDValue ct43 = CrtDAG->getConstant(32640, DL, MVT::i16, true, false);
// R19 = 32640;// Instr #134
SDNode *vload25 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct43,
                                    SDValue(vload23, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq10, 0)
                                    );

// END_WHERE;// Instr #135
SDNode *endwhere10 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload25, 1)
                                    );

// R39 = R36 | R35;// Instr #136
SDNode *or11 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq18, 0),
                                    SDValue(eq17, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere10, 0)
                                    );

// R04 = R61 == R31;// Instr #137
SDNode *eq23 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or10, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(or11, 1)
                                    );

// R03 = R39 == R30;// Instr #138
SDNode *eq24 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or11, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq23, 1)
                                    );

// R03 = R03 & R04;// Instr #139
SDNode *and19 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq23, 0),
                                    SDValue(eq24, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq24, 1)
                                    );

// R03 = R03 == R30;// Instr #140
SDNode *eq25 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and19, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and19, 1)
                                    );

SDValue ct44 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #141
SDNode *nop14 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct44,
                                    // glue (or chain) input edge
                                    SDValue(eq25, 1)
                                    );

// WHERE_EQ;// Instr #142
SDNode *whereeq11 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop14, 0)
                                    );

// R19 = R19 | R15;// Instr #143
SDNode *or12 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor0, 0),
                                    SDValue(vload25, 0),
                                    SDValue(vload25, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq11, 0)
                                    }
                                    );

SDValue ct45 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #144
SDNode *vload26 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct45,
                                    SDValue(vload24, 0),
                                    // glue (or chain) input edge
                                    SDValue(or12, 1)
                                    );

// END_WHERE;// Instr #145
SDNode *endwhere11 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload26, 1)
                                    );

// R52 = R00 == R30;// Instr #146
SDNode *eq26 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere11, 0)
                                    );

SDValue ct46 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #147
SDNode *nop15 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct46,
                                    // glue (or chain) input edge
                                    SDValue(eq26, 1)
                                    );

// WHERE_EQ;// Instr #148
SDNode *whereeq12 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop15, 0)
                                    );

// R16 = R21 + R25;// Instr #149
SDNode *add0 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload11, 0),
                                    SDValue(vload12, 0),
                                    SDValue(vload15, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq12, 0)
                                    }
                                    );

SDValue ct47 = CrtDAG->getConstant(127, DL, MVT::i16, true, false);
// R04 = 127;// Instr #150
SDNode *vload27 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct47,
                                    SDValue(eq23, 0),
                                    // glue (or chain) input edge
                                    SDValue(add0, 1)
                                    );

// R16 = R16 - R04;// Instr #151
SDNode *sub2 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add0, 0),
                                    SDValue(vload27, 0),
                                    SDValue(add0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload27, 1)
                                    }
                                    );

// MULT_U(R42, R43);// Instr #152
SDNode *mult_u0 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(or4, 0),
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub2, 1)
                                    );

// R18 = MULT_LOW();// Instr #153
SDNode *multlo0 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload16, 0),
                                    // glue (or chain) input edge
                                    SDValue(mult_u0, 0)
                                    );

// R17 = MULT_HIGH();// Instr #154
SDNode *multhi0 = CrtDAG->getMachineNode(
                                    Connex::MULTHI_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload17, 0),
                                    // glue (or chain) input edge
                                    SDValue(multlo0, 1)
                                    );

// MULT_U(R22, R43);// Instr #155
SDNode *mult_u1 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(or7, 0),
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(multhi0, 1)
                                    );

// R33 = MULT_LOW();// Instr #156
SDNode *multlo1 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(, 0),
                                    // glue (or chain) input edge
                                    SDValue(mult_u1, 0)
                                    );

// R13 = MULT_HIGH();// Instr #157
SDNode *multhi1 = CrtDAG->getMachineNode(
                                    Connex::MULTHI_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq9, 0),
                                    // glue (or chain) input edge
                                    SDValue(multlo1, 1)
                                    );

// MULT_U(R42, R26);// Instr #158
SDNode *mult_u2 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(or4, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(multhi1, 1)
                                    );

// R32 = MULT_LOW();// Instr #159
SDNode *multlo2 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(, 0),
                                    // glue (or chain) input edge
                                    SDValue(mult_u2, 0)
                                    );

// R14 = MULT_HIGH();// Instr #160
SDNode *multhi2 = CrtDAG->getMachineNode(
                                    Connex::MULTHI_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq12, 0),
                                    // glue (or chain) input edge
                                    SDValue(multlo2, 1)
                                    );

// MULT_U(R22, R26);// Instr #161
SDNode *mult_u3 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(or7, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(multhi2, 1)
                                    );

// R12 = MULT_LOW();// Instr #162
SDNode *multlo3 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(mult_u3, 0)
                                    );

// R17 = R33 + R17;// Instr #163
SDNode *add1 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(multhi0, 0),
                                    SDValue(multlo1, 0),
                                    SDValue(multhi0, 0),
                                    // glue (or chain) input edge
                                    SDValue(multlo3, 1)
                                    }
                                    );

// R04 = ADDC(R31, R31);// Instr #164
SDNode *addc0 = CrtDAG->getMachineNode(
                                    Connex::ADDCV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(vload0, 0),
                                    SDValue(add1, 0),
                                    SDValue(vload27, 0)
                                    // no need for glue or chain input (since it normally consumes the output of the predecessor)
                                    }
                                    );

// R17 = R32 + R17;// Instr #165
SDNode *add2 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add1, 0),
                                    SDValue(multlo2, 0),
                                    SDValue(add1, 0),
                                    // glue (or chain) input edge
                                    SDValue(addc0, 1)
                                    }
                                    );

// R33 = ADDC(R31, R31);// Instr #166
SDNode *addc1 = CrtDAG->getMachineNode(
                                    Connex::ADDCV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(vload0, 0),
                                    SDValue(add2, 0),
                                    SDValue(multlo1, 0)
                                    // no need for glue or chain input (since it normally consumes the output of the predecessor)
                                    }
                                    );

// R33 = R04 + R33;// Instr #167
SDNode *add3 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(addc1, 0),
                                    SDValue(addc0, 0),
                                    SDValue(addc1, 0),
                                    // glue (or chain) input edge
                                    SDValue(addc1, 1)
                                    }
                                    );

// R33 = R13 + R33;// Instr #168
SDNode *add4 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add3, 0),
                                    SDValue(multhi1, 0),
                                    SDValue(add3, 0),
                                    // glue (or chain) input edge
                                    SDValue(add3, 1)
                                    }
                                    );

// R33 = R14 + R33;// Instr #169
SDNode *add5 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add4, 0),
                                    SDValue(multhi2, 0),
                                    SDValue(add4, 0),
                                    // glue (or chain) input edge
                                    SDValue(add4, 1)
                                    }
                                    );

// R33 = R12 + R33;// Instr #170
SDNode *add6 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add5, 0),
                                    SDValue(multlo3, 0),
                                    SDValue(add5, 0),
                                    // glue (or chain) input edge
                                    SDValue(add5, 1)
                                    }
                                    );

// END_WHERE;// Instr #171
SDNode *endwhere12 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add6, 1)
                                    );

SDValue ct48 = CrtDAG->getConstant(16, DL, MVT::i16, true, false);
// R03 = 16;// Instr #172
SDNode *vload28 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct48,
                                    // glue (or chain) input edge
                                    SDValue(endwhere12, 0)
                                    );

SDValue ct49 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R06 = R17 << 0;// Instr #173
SDNode *ishl2 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add2, 0),
                                    ct49,
                                    // glue (or chain) input edge
                                    SDValue(vload28, 1)
                                    );

// R04 = ULT(R31, R33);// Instr #174
SDNode *ult0 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(add6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl2, 1)
                                    );

// R52 = R04 & R00;// Instr #175
SDNode *and20 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(ult0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult0, 1)
                                    );

// R52 = R52 == R30;// Instr #176
SDNode *eq27 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and20, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and20, 1)
                                    );

SDValue ct50 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #177
SDNode *nop16 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct50,
                                    // glue (or chain) input edge
                                    SDValue(eq27, 1)
                                    );

// WHERE_EQ;// Instr #178
SDNode *whereeq13 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop16, 0)
                                    );

SDValue ct51 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R06 = R33 << 0;// Instr #179
SDNode *ishl3 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add6, 0),
                                    ct51,
                                    SDValue(ishl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq13, 0)
                                    }
                                    );

SDValue ct52 = CrtDAG->getConstant(48, DL, MVT::i16, true, false);
// R03 = 48;// Instr #180
SDNode *vload29 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct52,
                                    SDValue(vload28, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl3, 1)
                                    );

// END_WHERE;// Instr #181
SDNode *endwhere13 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload29, 1)
                                    );

SDValue ct53 = CrtDAG->getConstant(1, DL, MVT::i16, true, false);
// R04 = R06 >> 1;// Instr #182
SDNode *ishr2 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl3, 0),
                                    ct53,
                                    // glue (or chain) input edge
                                    SDValue(endwhere13, 0)
                                    );

// R06 = R06 | R04;// Instr #183
SDNode *or13 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr2, 0),
                                    SDValue(ishl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr2, 1)
                                    );

SDValue ct54 = CrtDAG->getConstant(2, DL, MVT::i16, true, false);
// R04 = R06 >> 2;// Instr #184
SDNode *ishr3 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or13, 0),
                                    ct54,
                                    // glue (or chain) input edge
                                    SDValue(or13, 1)
                                    );

// R06 = R06 | R04;// Instr #185
SDNode *or14 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr3, 0),
                                    SDValue(or13, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr3, 1)
                                    );

SDValue ct55 = CrtDAG->getConstant(4, DL, MVT::i16, true, false);
// R04 = R06 >> 4;// Instr #186
SDNode *ishr4 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or14, 0),
                                    ct55,
                                    // glue (or chain) input edge
                                    SDValue(or14, 1)
                                    );

// R06 = R06 | R04;// Instr #187
SDNode *or15 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr4, 0),
                                    SDValue(or14, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr4, 1)
                                    );

SDValue ct56 = CrtDAG->getConstant(8, DL, MVT::i16, true, false);
// R04 = R06 >> 8;// Instr #188
SDNode *ishr5 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or15, 0),
                                    ct56,
                                    // glue (or chain) input edge
                                    SDValue(or15, 1)
                                    );

// R06 = R06 | R04;// Instr #189
SDNode *or16 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr5, 0),
                                    SDValue(or15, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr5, 1)
                                    );

// R06 = ~R06;// Instr #190
SDNode *not0 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or16, 0),
                                    // glue (or chain) input edge
                                    SDValue(or16, 1)
                                    );

// R05 = POPCNT(R06);// Instr #191
SDNode *popcnt0 = CrtDAG->getMachineNode(
                                    Connex::POPCNT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not0, 0),
                                    // glue (or chain) input edge
                                    SDValue(not0, 1)
                                    );

// R05 = R03 - R05;// Instr #192
SDNode *sub3 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload29, 0),
                                    SDValue(popcnt0, 0),
                                    // glue (or chain) input edge
                                    SDValue(popcnt0, 1)
                                    );

SDValue ct57 = CrtDAG->getConstant(24, DL, MVT::i16, true, false);
// R04 = 24;// Instr #193
SDNode *vload30 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct57,
                                    // glue (or chain) input edge
                                    SDValue(sub3, 1)
                                    );

// R04 = R05 - R04;// Instr #194
SDNode *sub4 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    SDValue(vload30, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload30, 1)
                                    );

// R12 = R04 < R31;// Instr #195
SDNode *lt2 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub4, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub4, 1)
                                    );

// R52 = R12 & R00;// Instr #196
SDNode *and21 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(lt2, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt2, 1)
                                    );

// R52 = R52 == R30;// Instr #197
SDNode *eq28 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and21, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and21, 1)
                                    );

SDValue ct58 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #198
SDNode *nop17 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct58,
                                    // glue (or chain) input edge
                                    SDValue(eq28, 1)
                                    );

// WHERE_EQ;// Instr #199
SDNode *whereeq14 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop17, 0)
                                    );

// R03 = R31 - R04;// Instr #200
SDNode *sub5 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(sub4, 0),
                                    SDValue(vload29, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq14, 0)
                                    }
                                    );

// R18 = R18 << R03;// Instr #201
SDNode *shl0 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(multlo0, 0),
                                    SDValue(sub5, 0),
                                    SDValue(multlo0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub5, 1)
                                    }
                                    );

// END_WHERE;// Instr #202
SDNode *endwhere14 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(shl0, 1)
                                    );

// R12 = R31 < R04;// Instr #203
SDNode *lt3 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(sub4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere14, 0)
                                    );

// R52 = R12 & R00;// Instr #204
SDNode *and22 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(lt3, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt3, 1)
                                    );

// R52 = R52 == R30;// Instr #205
SDNode *eq29 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and22, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and22, 1)
                                    );

SDValue ct59 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #206
SDNode *nop18 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct59,
                                    // glue (or chain) input edge
                                    SDValue(eq29, 1)
                                    );

// WHERE_EQ;// Instr #207
SDNode *whereeq15 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop18, 0)
                                    );

SDValue ct60 = CrtDAG->getConstant(15, DL, MVT::i16, true, false);
// R03 = 15;// Instr #208
SDNode *vload31 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct60,
                                    SDValue(sub5, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq15, 0)
                                    );

// R12 = R03 < R04;// Instr #209
SDNode *lt4 = CrtDAG->getMachineNode(
                                    Connex::LT_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload31, 0),
                                    SDValue(sub4, 0),
                                    SDValue(lt3, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload31, 1)
                                    }
                                    );

// R14 = R16 < R31;// Instr #210
SDNode *lt5 = CrtDAG->getMachineNode(
                                    Connex::LT_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub2, 0),
                                    SDValue(vload0, 0),
                                    SDValue(multhi2, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt4, 1)
                                    }
                                    );

// R14 = R14 & R12;// Instr #211
SDNode *and23 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(lt4, 0),
                                    SDValue(lt5, 0),
                                    SDValue(lt5, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt5, 1)
                                    }
                                    );

// R14 = R14 == R30;// Instr #212
SDNode *eq30 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and23, 0),
                                    SDValue(vload1, 0),
                                    SDValue(and23, 0),
                                    // glue (or chain) input edge
                                    SDValue(and23, 1)
                                    }
                                    );

SDValue ct61 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #213
SDNode *nop19 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct61,
                                    // glue (or chain) input edge
                                    SDValue(eq30, 1)
                                    );

// WHERE_EQ;// Instr #214
SDNode *whereeq16 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop19, 0)
                                    );

SDValue ct62 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R18 = R17 << 0;// Instr #215
SDNode *ishl4 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add2, 0),
                                    ct62,
                                    SDValue(shl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq16, 0)
                                    }
                                    );

SDValue ct63 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R17 = R33 << 0;// Instr #216
SDNode *ishl5 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add6, 0),
                                    ct63,
                                    SDValue(add2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl4, 1)
                                    }
                                    );

SDValue ct64 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R33 = 0;// Instr #217
SDNode *vload32 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct64,
                                    SDValue(add6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl5, 1)
                                    );

// END_WHERE;// Instr #218
SDNode *endwhere15 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload32, 1)
                                    );

// R04 = R04 & R03;// Instr #219
SDNode *and24 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload31, 0),
                                    SDValue(sub4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere15, 0)
                                    );

SDValue ct65 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R03 = R33 << 0;// Instr #220
SDNode *ishl6 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload32, 0),
                                    ct65,
                                    // glue (or chain) input edge
                                    SDValue(and24, 1)
                                    );

// R33 = R33 >> R04;// Instr #221
SDNode *shr0 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload32, 0),
                                    SDValue(and24, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl6, 1)
                                    );

SDValue ct66 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R53 = R04 << 0;// Instr #222
SDNode *ishl7 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and24, 0),
                                    ct66,
                                    // glue (or chain) input edge
                                    SDValue(shr0, 1)
                                    );

// R04 = R29 - R04;// Instr #223
SDNode *sub6 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(and24, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl7, 1)
                                    );

SDValue ct67 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #224
SDNode *ishl8 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl4, 0),
                                    ct67,
                                    // glue (or chain) input edge
                                    SDValue(sub6, 1)
                                    );

// R55 = R55 << R04;// Instr #225
SDNode *shl1 = CrtDAG->getMachineNode(
                                    Connex::SHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl8, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl8, 1)
                                    );

// R55 = R55 >> R04;// Instr #226
SDNode *shr1 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shl1, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl1, 1)
                                    );

SDValue ct68 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R54 = R53 << 0;// Instr #227
SDNode *ishl9 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl7, 0),
                                    ct68,
                                    // glue (or chain) input edge
                                    SDValue(shr1, 1)
                                    );

// R12 = R17 << R04;// Instr #228
SDNode *shl2 = CrtDAG->getMachineNode(
                                    Connex::SHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl5, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl9, 1)
                                    );

// R17 = R17 >> R53;// Instr #229
SDNode *shr2 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl5, 0),
                                    SDValue(ishl7, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl2, 1)
                                    );

// R03 = R03 << R04;// Instr #230
SDNode *shl3 = CrtDAG->getMachineNode(
                                    Connex::SHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl6, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr2, 1)
                                    );

// R17 = R17 | R03;// Instr #231
SDNode *or17 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shl3, 0),
                                    SDValue(shr2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl3, 1)
                                    );

// R18 = R18 >> R53;// Instr #232
SDNode *shr3 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl4, 0),
                                    SDValue(ishl7, 0),
                                    // glue (or chain) input edge
                                    SDValue(or17, 1)
                                    );

// R18 = R18 | R12;// Instr #233
SDNode *or18 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shl2, 0),
                                    SDValue(shr3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr3, 1)
                                    );

// END_WHERE;// Instr #234
SDNode *endwhere16 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or18, 1)
                                    );

// R52 = R00 == R30;// Instr #235
SDNode *eq31 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere16, 0)
                                    );

SDValue ct69 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #236
SDNode *nop20 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct69,
                                    // glue (or chain) input edge
                                    SDValue(eq31, 1)
                                    );

// WHERE_EQ;// Instr #237
SDNode *whereeq17 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop20, 0)
                                    );

SDValue ct70 = CrtDAG->getConstant(47, DL, MVT::i16, true, false);
// R04 = 47;// Instr #238
SDNode *vload33 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct70,
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq17, 0)
                                    );

// R04 = R04 - R05;// Instr #239
SDNode *sub7 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload33, 0),
                                    SDValue(sub3, 0),
                                    SDValue(vload33, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload33, 1)
                                    }
                                    );

// R16 = R16 - R04;// Instr #240
SDNode *sub8 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub2, 0),
                                    SDValue(sub7, 0),
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub7, 1)
                                    }
                                    );

// END_WHERE;// Instr #241
SDNode *endwhere17 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub8, 1)
                                    );

SDValue ct71 = CrtDAG->getConstant(15, DL, MVT::i16, true, false);
// R03 = 15;// Instr #242
SDNode *vload34 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct71,
                                    // glue (or chain) input edge
                                    SDValue(endwhere17, 0)
                                    );

// R04 = R30 - R16;// Instr #243
SDNode *sub9 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload34, 1)
                                    );

// R12 = R03 < R04;// Instr #244
SDNode *lt6 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload34, 0),
                                    SDValue(sub9, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub9, 1)
                                    );

// R52 = R12 & R00;// Instr #245
SDNode *and25 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(lt6, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt6, 1)
                                    );

// R52 = R52 == R30;// Instr #246
SDNode *eq32 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and25, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and25, 1)
                                    );

SDValue ct72 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #247
SDNode *nop21 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct72,
                                    // glue (or chain) input edge
                                    SDValue(eq32, 1)
                                    );

// WHERE_EQ;// Instr #248
SDNode *whereeq18 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop21, 0)
                                    );

// R54 = R55 == R31;// Instr #249
SDNode *eq33 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr1, 0),
                                    SDValue(vload0, 0),
                                    SDValue(ishl9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq18, 0)
                                    }
                                    );

// R54 = R30 - R54;// Instr #250
SDNode *sub10 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq33, 0),
                                    SDValue(eq33, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq33, 1)
                                    }
                                    );

SDValue ct73 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #251
SDNode *ishl10 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or18, 0),
                                    ct73,
                                    SDValue(shr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub10, 1)
                                    }
                                    );

// R55 = R55 | R54;// Instr #252
SDNode *or19 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub10, 0),
                                    SDValue(ishl10, 0),
                                    SDValue(ishl10, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl10, 1)
                                    }
                                    );

SDValue ct74 = CrtDAG->getConstant(16, DL, MVT::i16, true, false);
// R54 = 16;// Instr #253
SDNode *vload35 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct74,
                                    SDValue(sub10, 0),
                                    // glue (or chain) input edge
                                    SDValue(or19, 1)
                                    );

SDValue ct75 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R18 = R17 << 0;// Instr #254
SDNode *ishl11 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or17, 0),
                                    ct75,
                                    SDValue(or18, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload35, 1)
                                    }
                                    );

SDValue ct76 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R17 = R33 << 0;// Instr #255
SDNode *ishl12 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr0, 0),
                                    ct76,
                                    SDValue(or17, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl11, 1)
                                    }
                                    );

// R04 = R04 - R29;// Instr #256
SDNode *sub11 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub9, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub9, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl12, 1)
                                    }
                                    );

// R16 = R29 + R16;// Instr #257
SDNode *add7 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub8, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub11, 1)
                                    }
                                    );

// END_WHERE;// Instr #258
SDNode *endwhere18 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add7, 1)
                                    );

// R12 = R03 < R04;// Instr #259
SDNode *lt7 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload34, 0),
                                    SDValue(sub11, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere18, 0)
                                    );

// R52 = R12 & R00;// Instr #260
SDNode *and26 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(lt7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt7, 1)
                                    );

// R52 = R52 == R30;// Instr #261
SDNode *eq34 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and26, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and26, 1)
                                    );

SDValue ct77 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #262
SDNode *nop22 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct77,
                                    // glue (or chain) input edge
                                    SDValue(eq34, 1)
                                    );

// WHERE_EQ;// Instr #263
SDNode *whereeq19 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop22, 0)
                                    );

// R54 = R55 == R31;// Instr #264
SDNode *eq35 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or19, 0),
                                    SDValue(vload0, 0),
                                    SDValue(vload35, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq19, 0)
                                    }
                                    );

// R54 = R30 - R54;// Instr #265
SDNode *sub12 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq35, 0),
                                    SDValue(eq35, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq35, 1)
                                    }
                                    );

SDValue ct78 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #266
SDNode *ishl13 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl11, 0),
                                    ct78,
                                    SDValue(or19, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub12, 1)
                                    }
                                    );

// R55 = R55 | R54;// Instr #267
SDNode *or20 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub12, 0),
                                    SDValue(ishl13, 0),
                                    SDValue(ishl13, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl13, 1)
                                    }
                                    );

SDValue ct79 = CrtDAG->getConstant(16, DL, MVT::i16, true, false);
// R54 = 16;// Instr #268
SDNode *vload36 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct79,
                                    SDValue(sub12, 0),
                                    // glue (or chain) input edge
                                    SDValue(or20, 1)
                                    );

SDValue ct80 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #269
SDNode *vload37 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct80,
                                    SDValue(ishl11, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload36, 1)
                                    );

// R04 = R04 - R29;// Instr #270
SDNode *sub13 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub11, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub11, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload37, 1)
                                    }
                                    );

// R16 = R29 + R16;// Instr #271
SDNode *add8 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add7, 0),
                                    SDValue(vload2, 0),
                                    SDValue(add7, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub13, 1)
                                    }
                                    );

// END_WHERE;// Instr #272
SDNode *endwhere19 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add8, 1)
                                    );

// R12 = R16 < R30;// Instr #273
SDNode *lt8 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add8, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere19, 0)
                                    );

// R52 = R12 & R00;// Instr #274
SDNode *and27 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(lt8, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt8, 1)
                                    );

// R52 = R52 == R30;// Instr #275
SDNode *eq36 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and27, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and27, 1)
                                    );

SDValue ct81 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #276
SDNode *nop23 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct81,
                                    // glue (or chain) input edge
                                    SDValue(eq36, 1)
                                    );

// WHERE_EQ;// Instr #277
SDNode *whereeq20 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop23, 0)
                                    );

// R04 = R30 - R16;// Instr #278
SDNode *sub14 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(add8, 0),
                                    SDValue(sub13, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq20, 0)
                                    }
                                    );

// R54 = R55 == R31;// Instr #279
SDNode *eq37 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or20, 0),
                                    SDValue(vload0, 0),
                                    SDValue(vload36, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub14, 1)
                                    }
                                    );

// R54 = R30 - R54;// Instr #280
SDNode *sub15 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq37, 0),
                                    SDValue(eq37, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq37, 1)
                                    }
                                    );

SDValue ct82 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #281
SDNode *ishl14 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload37, 0),
                                    ct82,
                                    SDValue(or20, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub15, 1)
                                    }
                                    );

SDValue ct83 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R58 = R54 << 0;// Instr #282
SDNode *ishl15 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub15, 0),
                                    ct83,
                                    SDValue(vload21, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl14, 1)
                                    }
                                    );

// R03 = R29 - R04;// Instr #283
SDNode *sub16 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub14, 0),
                                    SDValue(vload34, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl15, 1)
                                    }
                                    );

// R55 = R55 << R03;// Instr #284
SDNode *shl4 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl14, 0),
                                    SDValue(sub16, 0),
                                    SDValue(ishl14, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub16, 1)
                                    }
                                    );

// R55 = R55 >> R03;// Instr #285
SDNode *shr4 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl4, 0),
                                    SDValue(sub16, 0),
                                    SDValue(shl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl4, 1)
                                    }
                                    );

SDValue ct84 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R54 = R04 << 0;// Instr #286
SDNode *ishl16 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub14, 0),
                                    ct84,
                                    SDValue(sub15, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr4, 1)
                                    }
                                    );

// R18 = R18 >> R04;// Instr #287
SDNode *shr5 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload37, 0),
                                    SDValue(sub14, 0),
                                    SDValue(vload37, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl16, 1)
                                    }
                                    );

SDValue ct85 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R03 = R17 << 0;// Instr #288
SDNode *ishl17 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl12, 0),
                                    ct85,
                                    SDValue(sub16, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr5, 1)
                                    }
                                    );

// R17 = R17 >> R04;// Instr #289
SDNode *shr6 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl12, 0),
                                    SDValue(sub14, 0),
                                    SDValue(ishl12, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl17, 1)
                                    }
                                    );

// R04 = R29 - R04;// Instr #290
SDNode *sub17 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub14, 0),
                                    SDValue(sub14, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr6, 1)
                                    }
                                    );

// R03 = R03 << R04;// Instr #291
SDNode *shl5 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl17, 0),
                                    SDValue(sub17, 0),
                                    SDValue(ishl17, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub17, 1)
                                    }
                                    );

// R18 = R18 | R03;// Instr #292
SDNode *or21 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl5, 0),
                                    SDValue(shr5, 0),
                                    SDValue(shr5, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl5, 1)
                                    }
                                    );

SDValue ct86 = CrtDAG->getConstant(1, DL, MVT::i16, true, false);
// R16 = 1;// Instr #293
SDNode *vload38 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct86,
                                    SDValue(add8, 0),
                                    // glue (or chain) input edge
                                    SDValue(or21, 1)
                                    );

// END_WHERE;// Instr #294
SDNode *endwhere20 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload38, 1)
                                    );

SDValue ct87 = CrtDAG->getConstant(254, DL, MVT::i16, true, false);
// R12 = 254;// Instr #295
SDNode *vload39 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct87,
                                    // glue (or chain) input edge
                                    SDValue(endwhere20, 0)
                                    );

// R12 = R12 < R16;// Instr #296
SDNode *lt9 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload39, 0),
                                    SDValue(vload38, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload39, 1)
                                    );

// R52 = R12 & R00;// Instr #297
SDNode *and28 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(lt9, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt9, 1)
                                    );

// R52 = R52 == R30;// Instr #298
SDNode *eq38 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and28, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and28, 1)
                                    );

SDValue ct88 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #299
SDNode *nop24 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct88,
                                    // glue (or chain) input edge
                                    SDValue(eq38, 1)
                                    );

// WHERE_EQ;// Instr #300
SDNode *whereeq21 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop24, 0)
                                    );

// R19 = R19 | R15;// Instr #301
SDNode *or22 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor0, 0),
                                    SDValue(or12, 0),
                                    SDValue(or12, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq21, 0)
                                    }
                                    );

SDValue ct89 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #302
SDNode *vload40 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct89,
                                    SDValue(vload26, 0),
                                    // glue (or chain) input edge
                                    SDValue(or22, 1)
                                    );

// END_WHERE;// Instr #303
SDNode *endwhere21 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload40, 1)
                                    );

SDValue ct90 = CrtDAG->getConstant(128, DL, MVT::i16, true, false);
// R04 = 128;// Instr #304
SDNode *vload41 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct90,
                                    // glue (or chain) input edge
                                    SDValue(endwhere21, 0)
                                    );

// R04 = R17 < R04;// Instr #305
SDNode *lt10 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr6, 0),
                                    SDValue(vload41, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload41, 1)
                                    );

// R12 = R16 == R30;// Instr #306
SDNode *eq39 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload38, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt10, 1)
                                    );

// R12 = R12 & R04;// Instr #307
SDNode *and29 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt10, 0),
                                    SDValue(eq39, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq39, 1)
                                    );

// R04 = R33 == R31;// Instr #308
SDNode *eq40 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr0, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and29, 1)
                                    );

// R12 = R12 & R04;// Instr #309
SDNode *and30 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq40, 0),
                                    SDValue(and29, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq40, 1)
                                    );

// R12 = R12 == R30;// Instr #310
SDNode *eq41 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and30, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and30, 1)
                                    );

// R52 = R12 & R00;// Instr #311
SDNode *and31 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload40, 0),
                                    SDValue(eq41, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq41, 1)
                                    );

// R52 = R52 == R30;// Instr #312
SDNode *eq42 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and31, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and31, 1)
                                    );

SDValue ct91 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #313
SDNode *nop25 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct91,
                                    // glue (or chain) input edge
                                    SDValue(eq42, 1)
                                    );

// WHERE_EQ;// Instr #314
SDNode *whereeq22 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop25, 0)
                                    );

SDValue ct92 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R16 = 0;// Instr #315
SDNode *vload42 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct92,
                                    SDValue(vload38, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq22, 0)
                                    );

// R33 = R17 | R17;// Instr #316
SDNode *or23 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr6, 0),
                                    SDValue(shr6, 0),
                                    SDValue(shr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload42, 1)
                                    }
                                    );

// R17 = R18 | R18;// Instr #317
SDNode *or24 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or21, 0),
                                    SDValue(or21, 0),
                                    SDValue(shr6, 0),
                                    // glue (or chain) input edge
                                    SDValue(or23, 1)
                                    }
                                    );

// END_WHERE;// Instr #318
SDNode *endwhere22 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or24, 1)
                                    );

// R12 = R30 < R16;// Instr #319
SDNode *lt11 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(vload42, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere22, 0)
                                    );

SDValue ct93 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #320
SDNode *nop26 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct93,
                                    // glue (or chain) input edge
                                    SDValue(lt11, 1)
                                    );

// WHERE_LT;// Instr #321
SDNode *wherelt0 = CrtDAG->getMachineNode(
                                    Connex::WHERELT,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop26, 0)
                                    );

SDValue ct94 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #322
SDNode *ishl18 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or21, 0),
                                    ct94,
                                    SDValue(shr4, 0),
                                    // glue (or chain) input edge
                                    SDValue(wherelt0, 0)
                                    }
                                    );

SDValue ct95 = CrtDAG->getConstant(16, DL, MVT::i16, true, false);
// R54 = 16;// Instr #323
SDNode *vload43 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct95,
                                    SDValue(ishl16, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl18, 1)
                                    );

SDValue ct96 = CrtDAG->getConstant(0, DL, MVT::i16, true, false);
// R18 = R17 << 0;// Instr #324
SDNode *ishl19 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or24, 0),
                                    ct96,
                                    SDValue(or21, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload43, 1)
                                    }
                                    );

// END_WHERE;// Instr #325
SDNode *endwhere23 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl19, 1)
                                    );

// R52 = R00 == R30;// Instr #326
SDNode *eq43 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload40, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere23, 0)
                                    );

SDValue ct97 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #327
SDNode *nop27 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct97,
                                    // glue (or chain) input edge
                                    SDValue(eq43, 1)
                                    );

// WHERE_EQ;// Instr #328
SDNode *whereeq23 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop27, 0)
                                    );

// R60 = R18 & R30;// Instr #329
SDNode *and32 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(ishl19, 0),
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq23, 0)
                                    }
                                    );

// R54 = R54 - R30;// Instr #330
SDNode *sub18 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload43, 0),
                                    SDValue(vload1, 0),
                                    SDValue(vload43, 0),
                                    // glue (or chain) input edge
                                    SDValue(and32, 1)
                                    }
                                    );

// R54 = R30 << R54;// Instr #331
SDNode *shl6 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(sub18, 0),
                                    SDValue(sub18, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub18, 1)
                                    }
                                    );

// R59 = R55 & R54;// Instr #332
SDNode *and33 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl6, 0),
                                    SDValue(ishl18, 0),
                                    SDValue(vload20, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl6, 1)
                                    }
                                    );

// R55 = R55 ^ R59;// Instr #333
SDNode *xor1 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and33, 0),
                                    SDValue(ishl18, 0),
                                    SDValue(ishl18, 0),
                                    // glue (or chain) input edge
                                    SDValue(and33, 1)
                                    }
                                    );

// R59 = R59 == R31;// Instr #334
SDNode *eq44 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and33, 0),
                                    SDValue(vload0, 0),
                                    SDValue(and33, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor1, 1)
                                    }
                                    );

// R59 = R30 - R59;// Instr #335
SDNode *sub19 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq44, 0),
                                    SDValue(eq44, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq44, 1)
                                    }
                                    );

// R55 = R55 == R31;// Instr #336
SDNode *eq45 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor1, 0),
                                    SDValue(vload0, 0),
                                    SDValue(xor1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub19, 1)
                                    }
                                    );

// R55 = R30 - R55;// Instr #337
SDNode *sub20 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq45, 0),
                                    SDValue(eq45, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq45, 1)
                                    }
                                    );

// R58 = R58 | R55;// Instr #338
SDNode *or25 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub20, 0),
                                    SDValue(ishl15, 0),
                                    SDValue(ishl15, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub20, 1)
                                    }
                                    );

// R57 = R58 | R60;// Instr #339
SDNode *or26 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and32, 0),
                                    SDValue(or25, 0),
                                    SDValue(vload22, 0),
                                    // glue (or chain) input edge
                                    SDValue(or25, 1)
                                    }
                                    );

// R57 = R57 & R59;// Instr #340
SDNode *and34 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub19, 0),
                                    SDValue(or26, 0),
                                    SDValue(or26, 0),
                                    // glue (or chain) input edge
                                    SDValue(or26, 1)
                                    }
                                    );

// END_WHERE;// Instr #341
SDNode *endwhere24 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and34, 1)
                                    );

SDValue ct98 = CrtDAG->getConstant(7, DL, MVT::i16, true, false);
// R04 = R16 << 7;// Instr #342
SDNode *ishl20 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload42, 0),
                                    ct98,
                                    // glue (or chain) input edge
                                    SDValue(endwhere24, 0)
                                    );

// CELL_SHR(R04, R30);// Instr #343
SDNode *cellshr1 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(ishl20, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl20, 1)
                                    );

SDValue ct99 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #344
SDNode *nop28 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct99,
                                    // glue (or chain) input edge
                                    SDValue(cellshr1, 0)
                                    );

// R04 = SHIFT_REG;// Instr #345
SDNode *ldsh3 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop28, 0)
                                    );

// R52 = R00 == R30;// Instr #346
SDNode *eq46 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload40, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh3, 1)
                                    );

SDValue ct100 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #347
SDNode *nop29 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct100,
                                    // glue (or chain) input edge
                                    SDValue(eq46, 1)
                                    );

// WHERE_EQ;// Instr #348
SDNode *whereeq24 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop29, 0)
                                    );

// R19 = R17 | R17;// Instr #349
SDNode *or27 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or24, 0),
                                    SDValue(or24, 0),
                                    SDValue(or22, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq24, 0)
                                    }
                                    );

// END_WHERE;// Instr #350
SDNode *endwhere25 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or27, 1)
                                    );

// CELL_SHR(R33, R30);// Instr #351
SDNode *cellshr2 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(or23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere25, 0)
                                    );

SDValue ct101 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #352
SDNode *nop30 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct101,
                                    // glue (or chain) input edge
                                    SDValue(cellshr2, 0)
                                    );

// R33 = SHIFT_REG;// Instr #353
SDNode *ldsh4 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop30, 0)
                                    );

// CELL_SHR(R00, R30);// Instr #354
SDNode *cellshr3 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(vload40, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh4, 1)
                                    );

SDValue ct102 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #355
SDNode *nop31 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct102,
                                    // glue (or chain) input edge
                                    SDValue(cellshr3, 0)
                                    );

// R52 = SHIFT_REG;// Instr #356
SDNode *ldsh5 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop31, 0)
                                    );

// CELL_SHR(R15, R30);// Instr #357
SDNode *cellshr4 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(xor0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh5, 1)
                                    );

SDValue ct103 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #358
SDNode *nop32 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct103,
                                    // glue (or chain) input edge
                                    SDValue(cellshr4, 0)
                                    );

// R15 = SHIFT_REG;// Instr #359
SDNode *ldsh6 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop32, 0)
                                    );

// R41 = INDEX;// Instr #360
SDNode *ldix6 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ldsh6, 1)
                                    );

// R38 = R41 & R30;// Instr #361
SDNode *and35 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix6, 1)
                                    );

// R34 = R38 == R30;// Instr #362
SDNode *eq47 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and35, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and35, 1)
                                    );

// R52 = R52 & R34;// Instr #363
SDNode *and36 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq47, 0),
                                    SDValue(ldsh5, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq47, 1)
                                    );

// R52 = R52 == R30;// Instr #364
SDNode *eq48 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and36, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and36, 1)
                                    );

SDValue ct104 = CrtDAG->getConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #365
SDNode *nop33 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct104,
                                    // glue (or chain) input edge
                                    SDValue(eq48, 1)
                                    );

// WHERE_EQ;// Instr #366
SDNode *whereeq25 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop33, 0)
                                    );

// R19 = R33 | R33;// Instr #367
SDNode *or28 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ldsh4, 0),
                                    SDValue(ldsh4, 0),
                                    SDValue(or27, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq25, 0)
                                    }
                                    );

// R19 = R19 & R10;// Instr #368
SDNode *and37 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload4, 0),
                                    SDValue(or28, 0),
                                    SDValue(or28, 0),
                                    // glue (or chain) input edge
                                    SDValue(or28, 1)
                                    }
                                    );

// R19 = R19 | R04;// Instr #369
SDNode *or29 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ldsh3, 0),
                                    SDValue(and37, 0),
                                    SDValue(and37, 0),
                                    // glue (or chain) input edge
                                    SDValue(and37, 1)
                                    }
                                    );

// R19 = R19 | R15;// Instr #370
SDNode *or30 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ldsh6, 0),
                                    SDValue(or29, 0),
                                    SDValue(or29, 0),
                                    // glue (or chain) input edge
                                    SDValue(or29, 1)
                                    }
                                    );

// END_WHERE;// Instr #371
SDNode *endwhere26 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or30, 1)
                                    );

// R19 = R57 + R19;// Instr #372
SDNode *add9 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or30, 0),
                                    SDValue(and34, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere26, 0)
                                    );

